name: Codecoverage-Main

# Runs PHPUnit unit and Codeception wp-browser wpunit tests, merges the code coverage, commits the html report to
# GitHub Pages, generates a README badge with the coverage percentage.

on:
  push:
    branches:
      - main
  pull_request:
    types: [ opened, reopened, ready_for_review, synchronize ]
    branches:
      - main
  workflow_dispatch:

# Disable permissions for all available scopes by default.
# Any needed permissions should be configured at the job level.
permissions: {}

jobs:
  get-repo-name:
    runs-on: ubuntu-latest
    permissions: {}
    outputs:
      repository-name: ${{ steps.repo-name.outputs.name }}
    steps:
      - name: Extract repository name
        id: repo-name
        # Extract just the repository name (e.g., "wp-module-coming-soon") from the full repository path
        # (e.g., "newfold-labs/wp-module-coming-soon") by splitting on '/' and taking the second field.
        run: echo "name=$(echo ${{ github.repository }} | cut -d'/' -f2)" >> $GITHUB_OUTPUT

  codecoverage:
    needs: get-repo-name
    permissions:
      contents: write      # Required by reusable codecoverage workflow (e.g., Pages/docs artifacts).
      pull-requests: write # Required by reusable codecoverage workflow for PR feedback.
    uses: newfold-labs/workflows/.github/workflows/reusable-codecoverage.yml@main
    with:
      php-versions: '["7.4", "8.0", "8.1", "8.2", "8.3", "8.4"]'
      coverage-php-version: '7.4'
      repository-name: ${{ needs.get-repo-name.outputs.repository-name }}
      minimum-coverage: 25
    secrets:
      repo_token: ${{ secrets.GITHUB_TOKEN }}
